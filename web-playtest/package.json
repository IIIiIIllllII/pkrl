{
  "name": "gen3-lut-human-playtest",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {"node": ">=22.18.0"},
  "scripts": {
    "predev": "node scripts/embed-simulator.mjs",
    "dev": "vite",
    "build:server": "node scripts/embed-simulator.mjs && esbuild server/simulator.ts --bundle --platform=node --format=cjs --target=node22 --outfile=server-dist/simulator.cjs",
    "build": "node scripts/embed-simulator.mjs && esbuild server/simulator.ts --bundle --platform=node --format=cjs --target=node22 --outfile=server-dist/simulator.cjs && tsc -b && vite build",
    "pretest": "node scripts/embed-simulator.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:simulator": "node scripts/verify-simulator.mjs",
    "export:policies": "../.venv/bin/python ../scripts/export_web_policies.py"
  },
  "dependencies": {
    "@vitejs/plugin-react": "6.1.1",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "ts-chacha20": "1.2.0",
    "vite": "8.3.0"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "typescript": "5.8.3",
    "esbuild": "0.28.2",
    "vitest": "5.0.1"
  }
}
